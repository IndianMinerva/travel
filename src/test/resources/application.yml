spring:
  sql:
    init:
      mode: always
  datasource:
    url: 'jdbc:tc:postgresql:///db'
    username: username
    password: password
  flyway:
    enabled: true
jpa:
  hibernate:
    ddl-auto: update
#    generate-ddl: true
#    show-sql: true